import {GameBaseCreationProps} from "@/GameController/GameBaseClass.ts";

// Here all the logic of the game is bundled into a single class that can be imported everywhere

export interface GameCreationProps extends GameBaseCreationProps {}
