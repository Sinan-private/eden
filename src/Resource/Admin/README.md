# Resource Management

The Admin part was created to enable easier resource management. By offering a UI to add and edit resources instead of
having to do all updates in complex objects. The Admin Tool also creates all keys and types dynamically. This way
development is also simplified.